# Agent Working Agreement

1. Read `PRD.md`, this file, `design.md`, `architecture.md`, `rules.md`, `decisions.md`, `memory.md`, and `testing.md` in that order before implementation.
2. Work in the current Vite/TanStack project until a separately approved Next.js migration phase begins.
3. Work in the phased order in `architecture.md`; finish and verify one slice before starting another.
4. Keep edits focused. Do not rewrite unrelated user changes or published history.
5. Whenever scope, decisions, structure, conventions, tests, or visual behavior changes, update the relevant project document in the same change.
6. Never guess payment, refund, notification, capacity, or authorization behavior when the requirements are unresolved. Check `PRD.md` and `memory.md`; ask the client when still unclear.
7. Report changed files, checks run, known failures, and unresolved questions honestly.

## Definition of done

- The requested slice works in the current app.
- Relevant tests or checks pass.
- Documentation reflects any changed decision or behavior.
- No secrets, deployment changes, or unrelated refactors were introduced.
